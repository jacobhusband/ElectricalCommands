# Shared Contracts & Assets

This directory contains shared assets, data contracts, schemas, and templates utilized across both `ElectricalCommands` and `ProjectManagement`:

- `schemas/`: JSON schemas describing panel schedules, circuiting contracts, and CAD scan exports.
- `templates/`: Common DWG, DXF, script, or configuration templates.
